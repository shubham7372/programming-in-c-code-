#include<stdio.h>
int main()
{
	int i, j;
	int x, y;
	int mat[x][y];
	printf("Enter the matrix");
	printf("Enter the value of x: ");
	scanf("%d",&x);
	printf("Enter the value of y: ");
	scanf("%d",&y);
	for(i=0; i<x; i++)
	{
		for(j=0, j=y, j++)
	}
	printf("%d,%d",i+1,j+1)
}